/**
 * @typedef {'todo' | 'in-progress' | 'done'} TaskStatus
 * @typedef {'high' | 'medium' | 'low'} Priority
 * @typedef {{ id: string, title: string, description: string, priority: Priority, status: TaskStatus, deadline: string }} Task
 */

export {}
